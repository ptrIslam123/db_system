
/* set sys_operators */

{
    {"get",      "функция выборки данных из таблицы, требует заранее определение таблицы через set_t"},
    {"insert",   "функция для вставки новых данных в заранее отобранных записях через вызов get"},
    {"update",   "функция изменения данных записей таблицы отобранных заранее функцией get"},
    {"remove",   "функция удаления записей из таблицы ,заранее определенных(отобранных) через функцию get"},
    {"create_t", "функция создания таблицы и заполнения ее данными из указанного файла"},
    {"drop_t",   "функция удаления записей таблицы и самой таблицы из базы данных"},
    {"set_t",    "функция определения таблицы в глобальном контексте с которой будут работать выше описанные функции"},
    {"trigger <TriggerName> { <TriggerBlock> }" , "Декларация тригера"},
    {"bef_attach(nm: '<TriggerName>' op : 'Event' )", "Функция для активации тригера"}
    {"bef_detach(nm : '<TriggerName>' )", "Функция для дезактивации тригера"}
}


/* semantics db */

GET          =  get( dt : 'data' || ti : 'data' || ds : 'data' || ps : 'index' )

INSERT       =  insert( dt : 'data' ti : 'data' ds : 'data' )

UPDATE       =  update( dt : 'data' || ti : 'data' || ds : 'data' )

REMOVE       =  remove( )

CREATE_TABLE =  create_t( tn : 'table_name' fn : 'file_name' sz : 'size_table' )

DROP_TABLE   =  drop_t( tn : 'table_name' )

SET_TABLE    =  set_t( tn : 'table_name' )



